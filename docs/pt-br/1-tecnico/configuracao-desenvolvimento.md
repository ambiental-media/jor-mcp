# Configuração de Desenvolvimento Local

*(Placeholder: Como rodar o ambiente de desenvolvimento usando `uv`, executar a suíte `make check` e rodar o servidor `uvicorn` e o portal Next.js localmente).*